{
  "name": "birdseye-quiz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Endless birds-eye-view city quiz. Static frontend, images sourced from Wikimedia Commons.",
  "scripts": {
    "build:cities": "node scripts/build-cities.mjs",
    "build:prune": "node scripts/prune-quality.mjs",
    "build:thumbs": "node scripts/add-small-thumbs.mjs",
    "build:slim": "node scripts/slim-manifest.mjs",
    "build": "node scripts/build-cities.mjs && node scripts/prune-quality.mjs && node scripts/add-small-thumbs.mjs && node scripts/slim-manifest.mjs",
    "dev": "npx --yes http-server -p 5173 -c-1 ."
  },
  "engines": {
    "node": ">=20"
  }
}
